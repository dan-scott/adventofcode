const std = @import("std");
const util = @import("../utils.zig");

pub const year = ;
pub const day = ;

pub fn part1solver(_: std.mem.Allocator, _: []const u8) ![]const u8 {
    return "Not implemented";
}

pub fn part2solver(_: std.mem.Allocator, _: []const u8) ![]const u8 {
    return "Not implemented";
}

test "part1Solver" {
    const cases = [_]struct { input: []const u8, expected: []const u8 }{
      
    };
    const allocator = std.testing.allocator;
    for (cases) |case| {
        const actual = try part1solver(allocator, case.input);
        defer allocator.free(actual);
        try std.testing.expectEqualStrings(case.expected, actual);
    } 
}

test "part2Solver" {
    const cases = [_]struct { input: []const u8, expected: []const u8 }{
      
    };
    const allocator = std.testing.allocator;
    for (cases) |case| {
        const actual = try part2solver(allocator, case.input);
        defer allocator.free(actual);
        try std.testing.expectEqualStrings(case.expected, actual);
    } 
}
